name: Test

on:
    [push, pull_request]                


jobs:
    Test:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@master
          - uses: denolib/setup-deno@v1.1.0
          - run: deno test