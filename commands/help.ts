export const show_help = () => {

    const message = `
        commands: 
            - init: initialize a new blog 
            - build: assemble the html pages 
            - help: show this page
    `;

    console.log(message);
}